All trade executions must be idempotent.

Durable Objects are source of truth.

D1 is projection layer only.

No external API call without timeout.

Every LLM output must pass schema validation.

Every state transition must be restart-safe.

All alarms must be re-schedulable.

No silent failure allowed.
